<!DOCTYPE html>
<html>
<head>
  <style>
    body {
      text-align: center;
      margin: 0 auto;
      max-width: 800px;
      padding: 20px;
      font-family: Arial, sans-serif;
      background-color: #f8f8f8;
    }

    .center {
      text-align: center;
      margin-bottom: 10px;
    }

    h1, h2 {
      color: #333;
    }

    .article-container {
      background-color: #fff; 
      padding: 20px;
      border-radius: 5px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      margin-bottom: 20px;
    }

    p {
      margin-top: 10px;
    }

    a {
      color: #007bff;
      text-decoration: none;
    }

    /* Style for comment form */
    .comment-form {
      margin-top: 20px;
      padding: 10px;
      background-color: #fff;
      border-radius: 5px;
      box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
    }

    .comment-form label {
      display: block;
      margin-bottom: 5px;
      font-weight: bold;
    }

    .comment-form input[type="text"],
    .comment-form textarea {
      width: 100%;
      padding: 8px;
      margin-bottom: 10px;
      border: 1px solid #ccc;
      border-radius: 3px;
    }

    .comment-form button {
      padding: 8px 15px;
      background-color: #007bff;
      color: #fff;
      border: none;
      border-radius: 3px;
      cursor: pointer;
    }

  </style>
</head>
<body>

  <div class="article-container">
    <h2><%= @article.title %></h2>
    <p><%= @article.content.html_safe %></p>
      <div class="center">
        <%= image_tag(@article.avatar, class: "avatar") if @article.avatar.present?%>
      </div>
        <p align="left">Oluşturulma Tarihi: <%= @article.created_at.strftime('%Y-%m-%d %H:%M:%S') %></p>
      <%= link_to 'Değiştir', edit_article_path(@article) %> |
      <%= link_to 'Geri', articles_path %>
  </div>

  <div class="comment-form">
    <%= render 'comments/form' %>
  </div>
  
  <%= render @article.comments %>
</body>
</html>
