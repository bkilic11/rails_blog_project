<!DOCTYPE html>
<html>
<head>
  <style>
    .comment-container {
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
      background-color: #fff;
      box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.1);
    }

    .comment {
      border-bottom: 1px solid #eee;
      padding: 10px 0;
      display: flex;
      flex-direction: column;
    }

    .comment-header {
      font-weight: bold;
      color: #333;
    }

    .comment-body {
      margin-top: 5px;
      color: #555;
    }

    .delete-button {
      margin-top: 10px;
    }

    .delete-button button {
      background-color: #e74c3c;
      color: white;
      border: none;
      padding: 6px 10px;
      border-radius: 4px;
      cursor: pointer;
    }
  </style>
</head>
<body>
<br>

  <% if comment.body.present? %>  
  <h2>Yorumlar</h2>
    <div class="comment-container">
      <div class="comment">
        <strong class="comment-header">Gönderen: <%= comment.commenter %></strong>
        <div class="comment-body"> Yorum: <%= comment.body %></div>
          <% if comment.created_at.present? %>
            <p class="comment-date">Yorum Tarihi: <%= comment.created_at.strftime('%d-%m-%Y %H:%M:%S') %></p>
          <% end %>
        <div class="delete-button">
          <%= button_to "Yorumu Sil", [comment.article, comment], method: :delete %>
        </div>
  <% end %>
</body>
</html>
